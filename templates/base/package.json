{
  "name": "streamx-addon",
  "version": "1.0.0",
  "type": "module",
  "description": "StreamX community addon",
  "main": "src/index.js",
  "scripts": {
    "start":    "node src/index.js",
    "dev":      "node --watch src/index.js",
    "test":     "streamx-deploy test",
    "deploy":   "streamx-deploy deploy",
    "validate": "streamx-deploy validate",
    "publish":  "streamx-deploy publish"
  },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "streamx-deploy": "^1.0.0"
  },
  "engines": { "node": ">=18" }
}
